// DRAFT FIRESTORE RULES - SATISFIES WORKFLOW PROCESS
// Hardened constraints validated and promoted to production in firestore.rules.

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
